# No-show Policy

No-show is manual only. It never runs from timeout, never automatically cancels an Order, and never releases quantity. A staff member may mark an unresolved Order `no_show`; a separate manually confirmed Release Inventory action may then restore the Event sellable quantity once, without changing the no-show status.
