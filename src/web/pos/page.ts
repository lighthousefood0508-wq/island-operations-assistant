import { renderRealtimeDebug } from "../shared/realtime-debug.js";

export function renderPos(): string {
  return `<!doctype html>
<html lang="zh-Hant">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>荒島餐車 POS</title>
  <style>
    :root{font-family:Arial,"Noto Sans TC",sans-serif;color:#173237;background:#eef3f0;--green:#145c57;--ink:#173237;--muted:#587075;--line:#c7d5d0;--paper:#fff;--warn:#b13f2e;--soft:#e5eee9;--lift:0 6px 18px rgba(20,60,54,.08)}*{box-sizing:border-box}body{margin:0}main{max-width:1440px;margin:auto;padding:10px}.topbar{display:grid;grid-template-columns:minmax(0,1fr) auto auto;gap:10px;align-items:center;padding:4px 0 8px}.topbar h1{font-size:24px;margin:0;letter-spacing:0}.event{margin:3px 0 0;color:var(--muted);font-size:13px}.system-menu{position:relative;z-index:30}.system-menu summary{list-style:none;border:1px solid var(--line);background:#fff;color:#24484b;border-radius:7px;padding:10px 15px;font-weight:900;cursor:pointer;box-shadow:var(--lift)}.system-menu summary::-webkit-details-marker{display:none}.system-links{position:absolute;right:0;top:46px;min-width:220px;display:grid;gap:6px;background:#fff;border:1px solid var(--line);border-radius:8px;padding:8px;box-shadow:0 14px 34px rgba(20,60,54,.18)}.system-links a,.system-links span{text-decoration:none;color:#24484b;border-radius:6px;padding:11px 12px;font-weight:900}.system-links a:hover{background:#e5eee9}.system-links .current,.system-links .disabled{background:#eef3f0;color:#72817e;cursor:not-allowed}.system-links .future{border-top:1px solid #e1e9e5;margin-top:2px;padding-top:10px}.header-metrics{display:grid;grid-template-columns:minmax(170px,1.45fr) repeat(5,minmax(82px,1fr));gap:7px;margin-bottom:8px}.header-metric{background:#fff;border:1px solid var(--line);border-radius:6px;padding:7px 9px}.header-metric span{display:block;color:var(--muted);font-size:11px;font-weight:700}.header-metric strong{display:block;font-size:17px;margin-top:1px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.tabs{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px;margin-bottom:8px}.tab{border:1px solid var(--line);background:#f8fbf9;color:#294b50;border-radius:6px;padding:11px 8px;font:900 16px Arial,sans-serif;cursor:pointer}.tab[aria-selected="true"]{background:var(--green);border-color:var(--green);color:#fff}.pane[hidden],#sync-debug[hidden]{display:none}.stats{display:none}.layout{display:grid;grid-template-columns:minmax(0,1fr) 360px;gap:10px;align-items:start}.panel,.product,.order{background:var(--paper);border:1px solid var(--line);border-radius:6px}.panel{padding:10px}.panel h2,.panel h3{margin:0 0 8px}.panel h2{font-size:18px}.category-tabs{display:flex;gap:6px;overflow-x:auto;margin-bottom:8px;padding-bottom:2px}.category-tab{background:#fff;border:1px solid var(--line);color:#294b50;border-radius:999px;padding:8px 12px;font:900 14px Arial,sans-serif;white-space:nowrap;cursor:pointer}.category-tab[aria-pressed="true"]{background:#d6ebe4;border-color:var(--green);color:#123c3a}.products{display:grid;gap:10px}.product-section{display:grid;gap:7px}.product-section h3{font-size:15px;color:#496368;margin:0}.product-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(128px,1fr));gap:8px}.product{min-height:108px;padding:9px;display:grid;grid-template-rows:auto auto auto 1fr;gap:3px;box-shadow:0 1px 0 rgba(20,60,54,.04)}.product.sold-out{background:#edf0ee;border-color:#d8dfdc;color:#71817e}.product-name{font-weight:900;font-size:19px;line-height:1.05;color:#104b50}.product-price{font-weight:900;font-size:17px}.product-meta{color:var(--muted);font-size:12px;font-weight:700}.soldout{color:var(--warn);font-weight:900}.product button{align-self:end;width:100%;font-weight:900;font-size:16px;padding:8px 10px}.cart{position:sticky;top:10px;align-self:start;border-color:#9ebbb2;box-shadow:var(--lift)}.cart h2{font-size:20px}.cart-time{display:flex;justify-content:space-between;gap:8px;margin:0 0 8px;color:#3e5d61;font-size:13px;font-weight:900}.cart-items{display:grid;gap:7px;max-height:28vh;overflow:auto;margin-top:8px}.cart-line{border:1px solid #dce5e1;border-radius:6px;padding:8px;background:#fbfdfc}.cart-main{display:grid;grid-template-columns:minmax(0,1fr) auto auto;gap:7px;align-items:center}.line-total{font-weight:900}.quantity{display:flex;align-items:center;gap:4px}.quantity span{min-width:28px;text-align:center;font-weight:900}.quantity button,.icon-button{min-width:32px;padding:7px 8px}.field{display:block;margin-top:8px;color:#3e5d61;font-size:13px;font-weight:700}.field input,.field select,.field textarea{display:block;width:100%;font:inherit;border:1px solid #b9cbc4;border-radius:5px;padding:8px;margin-top:4px;background:#fff}.field textarea{min-height:50px;resize:vertical}.cart-fields{display:grid;grid-template-columns:1fr 104px;gap:7px}.cart-fields .wide{grid-column:1/-1}.cart-footer{border-top:1px solid var(--line);margin-top:10px;padding-top:10px}.total-row{display:flex;align-items:center;justify-content:space-between;font-size:19px;font-weight:900;margin:8px 0}.primary,button{font:inherit;border:0;border-radius:5px;background:var(--green);color:#fff;padding:10px 13px;cursor:pointer}.primary{width:100%;font-size:18px;font-weight:900;padding:12px}.secondary{background:#617477}.danger{background:var(--warn)}.ghost{background:#e8efec;color:#24484b}.order-actions{display:flex;gap:6px;flex-wrap:wrap;margin-top:10px}button:disabled{cursor:not-allowed;opacity:.52}.notice{min-height:28px;margin:10px 0 0;color:var(--warn);display:flex;gap:8px;align-items:center;font-weight:800}.notice button{padding:5px 8px;background:#617477}.order-list{display:grid;gap:8px}.orders-toolbar{display:flex;justify-content:space-between;gap:12px;align-items:end;margin:0 0 10px}.orders-toolbar .field{margin-top:0;min-width:260px}.order{padding:11px}.order-top{display:flex;justify-content:space-between;gap:10px}.order-number{font-weight:900;font-size:18px}.order-meta,.order-items,.order-detail{font-size:13px;color:var(--muted);margin-top:5px}.order-items{color:#294b50;font-weight:700}.order-detail{border-top:1px solid #e1e9e5;padding-top:8px;margin-top:8px}.status-list{display:flex;gap:5px;flex-wrap:wrap;margin-top:7px}.status{font-size:12px;padding:4px 7px;border-radius:999px;background:#edf3f0;color:#36575b}.empty{color:var(--muted);padding:12px 0}.unavailable{max-width:680px;color:var(--muted)}@media(max-width:1120px){.header-metrics{grid-template-columns:repeat(3,minmax(0,1fr))}.layout{grid-template-columns:minmax(0,1fr) 335px}.product-grid{grid-template-columns:repeat(auto-fill,minmax(122px,1fr))}}@media(max-width:900px){main{padding:8px}.layout{grid-template-columns:1fr}.cart{position:static}.cart-items{max-height:none}.topbar{grid-template-columns:1fr auto}.topbar #connection-status{grid-column:1/-1}.tabs{grid-template-columns:repeat(3,minmax(0,1fr))}.orders-toolbar{align-items:stretch;flex-direction:column}.orders-toolbar .field{min-width:0}.product-grid{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:620px){.header-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.cart-fields{grid-template-columns:1fr}.product-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.product{min-height:104px}.product-name{font-size:18px}.product-price{font-size:16px}.tab{font-size:15px;padding:10px 8px}}
    .sync-status,.sync-debug-toggle{display:none!important}.eyebrow{color:#00796b;font-size:13px;font-weight:900;margin:0 0 2px}body{background:#f5f2ec}main{max-width:none;padding:14px}.topbar{display:flex;align-items:center;justify-content:space-between;gap:14px;padding:0 0 10px}.topbar h1{font-size:28px}.event{font-size:13px;font-weight:800}.system-menu summary{border:1px solid #a9d8d2;background:#e9fbf7;color:#00796b;border-radius:999px;padding:9px 15px;box-shadow:none}.system-links{position:absolute;right:0;top:46px;min-width:220px;display:grid;gap:6px;background:#fff;border:1px solid var(--line);border-radius:8px;padding:8px;box-shadow:0 14px 34px rgba(20,60,54,.18)}.system-links a,.system-links span{text-decoration:none;color:#24484b;border-radius:6px;padding:11px 12px;font-weight:900}.system-links a:hover{background:#e5eee9}.system-links .current,.system-links .disabled{background:#eef3f0;color:#72817e;cursor:not-allowed}.system-links .future{border-top:1px solid #e1e9e5;margin-top:2px;padding-top:10px}.header-metrics{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-bottom:10px}.header-metric:nth-child(1),.header-metric:nth-child(2),.header-metric:nth-child(6){display:none}.header-metric{border-color:#d8d2c8;border-radius:7px;padding:11px 13px}.header-metric span{font-size:13px}.header-metric strong{font-size:24px}.tabs{display:flex;gap:7px;overflow-x:auto;margin-bottom:10px}.tab{min-width:138px;border-color:transparent;background:#e8e0d4;color:#2c302f;border-radius:7px;padding:12px 16px}.tab[aria-selected="true"]{background:#0d796d;color:#fff}.layout{grid-template-columns:minmax(0,1fr) 300px;gap:12px}.panel{border-color:#d8d2c8;border-radius:7px;padding:13px}.panel h2{font-size:20px}.category-tabs{display:none}.products{gap:13px}.product-section{gap:8px}.product-section h3{color:#65717d;font-size:15px}.product-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.product{min-height:124px;padding:13px;display:grid;grid-template-columns:minmax(0,1fr) 48px;grid-template-rows:auto auto 1fr;gap:4px;border-color:#d8d2c8;border-radius:7px}.product:not(.sold-out){cursor:pointer}.product:not(.sold-out):active{background:#eef8f4}.product-name{font-size:25px;color:#082c30}.product-price{font-size:16px;color:#21363a}.product-meta{align-self:end;border-top:1px solid #d8d2c8;padding-top:8px}.product-cart-count{color:#0d796d}.product button{grid-column:2;grid-row:1/4;align-self:center;justify-self:end;width:auto;background:transparent;color:#b33f2f;font-size:18px;padding:8px 4px}.product button:disabled{color:#8a5a4a;background:transparent}.cart{top:12px;border-color:#d8d2c8;border-radius:7px;box-shadow:none}.cart h2{font-size:21px}.cart-time{justify-content:flex-end}.cart-time span{display:none}.cart-fields{grid-template-columns:1fr 88px}.field{font-size:12px}.field input,.field select,.field textarea{border-color:#d8d2c8;border-radius:6px;min-height:39px}.field textarea{min-height:46px}.payment-options{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:4px}.payment-options .payment-option{background:#f4f1eb;color:#24383b;border:1px solid #d8d2c8;font-weight:800}.payment-options .payment-option[aria-pressed="true"]{background:#e1f2ed;color:#0d675e;border-color:#0d796d}.cart-items{min-height:96px;max-height:23vh;border-bottom:1px solid #d8d2c8;padding-bottom:8px}.cart-items .empty{display:flex;align-items:center;justify-content:center;min-height:84px}.cart-line{border:0;border-bottom:1px solid #e5ddd1;border-radius:0;background:transparent;padding:7px 0}.cart-main{grid-template-columns:minmax(0,1fr) auto auto}.quantity button,.icon-button{min-width:30px;border-radius:5px}.cart-footer{border-top:0;margin-top:8px;padding-top:0}.secondary{background:#f4f1eb;color:#24383b;border:1px solid #d8d2c8}.primary{background:#0d796d;border-radius:6px;font-size:16px}.total-row{font-size:16px;margin:10px 0}.total-row strong{font-size:28px}.notice{font-size:13px;min-height:24px}.mobile-cart-summary{display:none;position:fixed;left:12px;right:12px;bottom:12px;z-index:40;align-items:center;justify-content:space-between;gap:12px;background:#0d796d;color:#fff;border-radius:7px;padding:13px 16px;box-shadow:0 8px 24px rgba(20,60,54,.24);font-weight:900}.mobile-cart-summary[hidden]{display:none!important}@media(max-width:1120px){main{padding:10px}.layout{grid-template-columns:minmax(0,1fr) 295px}.product-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.product-name{font-size:23px}}@media(max-width:900px){main{padding-bottom:82px}.topbar{align-items:flex-start}.header-metrics{grid-template-columns:repeat(3,minmax(0,1fr))}.layout{grid-template-columns:1fr}.cart{position:static}.product-grid{grid-template-columns:repeat(3,minmax(0,1fr))}.cart-items{max-height:none}.tabs{display:grid;grid-template-columns:repeat(3,minmax(0,1fr))}.tab{min-width:0}.mobile-cart-summary{display:flex}}@media(max-width:620px){.topbar h1{font-size:24px}.header-metrics{grid-template-columns:repeat(3,minmax(0,1fr))}.header-metric{padding:8px}.header-metric span{font-size:11px}.header-metric strong{font-size:18px}.product-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.product{min-height:112px}.product-name{font-size:20px}.cart-fields{grid-template-columns:1fr}.tabs{grid-template-columns:1fr 1fr 1fr}.tab{font-size:14px;padding:10px 6px}}
    .reservation-toggle{width:100%;margin-top:8px}.reservation-fields{border-top:1px solid var(--line);margin-top:8px;padding-top:8px}.reservation-fields[hidden]{display:none}.pickup-badge{display:inline-block;background:#fff0c9;color:#85530b;border-radius:999px;padding:4px 8px;font-size:12px;font-weight:900;margin-left:6px}.tabs{grid-template-columns:repeat(4,minmax(0,1fr))}@media(max-width:900px){.tabs{grid-template-columns:repeat(4,minmax(0,1fr))}}@media(max-width:620px){.tabs{grid-template-columns:repeat(2,minmax(0,1fr))}}
    .cash-calculator{display:grid;grid-template-columns:1fr 1fr;gap:7px;align-items:end}.cash-calculator[hidden]{display:none}.cash-change{min-height:39px;border:1px solid #b9cbc4;border-radius:6px;background:#f7faf8;padding:7px 9px;display:flex;align-items:center;justify-content:space-between;gap:8px}.cash-change strong{font-size:18px;color:#0d675e}.sync-status{display:flex!important;align-items:center;justify-content:flex-end;gap:7px}.sync-status #connection-status[data-state="connected"]{color:#167b53}.sync-status #connection-status[data-state="reconnecting"]{color:#9a6500}.sync-status #connection-status[data-state="offline"]{color:#b13f2e}.sync-device-label{display:none}.sync-debug-toggle{display:none!important}@media(max-width:900px){.sync-status{justify-content:flex-start}}@media(max-width:620px){.cash-calculator{grid-template-columns:1fr}}
    .collection-panel{border-top:1px solid #e1e9e5;margin-top:10px;padding-top:10px}.collection-panel strong{display:block;margin-bottom:6px}.collection-actions{display:grid;grid-template-columns:1fr 1fr minmax(140px,auto);gap:7px}.collection-actions button{background:#f4f1eb;color:#24383b;border:1px solid #d8d2c8;font-weight:800}.collection-actions button[aria-pressed="true"]{background:#e1f2ed;color:#0d675e;border-color:#0d796d}.collection-actions .confirm-payment{background:#0d796d;color:#fff;border-color:#0d796d}@media(max-width:620px){.collection-actions{grid-template-columns:1fr 1fr}.collection-actions .confirm-payment{grid-column:1/-1}}
  </style>
</head>
<body>
  <main>
    <header class="topbar">
      <div><p class="eyebrow">荒島餐車</p><h1>今日點餐系統</h1><p id="event" class="event">讀取場次中...</p></div>
      ${renderRealtimeDebug("POS")}
      <details class="system-menu">
        <summary>系統</summary>
        <nav class="system-links" aria-label="系統連結">
          <span class="current" aria-disabled="true">POS 點餐</span>
          <a href="/kitchen">廚房系統</a>
          <a href="/admin">後台管理</a>
        </nav>
      </details>
    </header>
    <section class="header-metrics" aria-label="POS 營業狀態">
      <article class="header-metric"><span>目前場次</span><strong id="header-event">-</strong></article>
      <article class="header-metric"><span>場次狀態</span><strong id="header-status">CLOSED</strong></article>
      <article class="header-metric"><span>剩餘主餐</span><strong id="header-remaining">0</strong></article>
      <article class="header-metric"><span>待出餐</span><strong id="header-pending">0</strong></article>
      <article class="header-metric"><span>預約數</span><strong id="header-preorder">0</strong></article>
      <article class="header-metric"><span>目前操作者</span><strong id="header-operator">Owner</strong></article>
    </section>
    <nav class="tabs" aria-label="POS 分頁">
      <button class="tab" data-tab="onsite" aria-selected="true">現場點餐</button>
      <button class="tab" data-tab="pending" aria-selected="false">待出餐</button>
      <button class="tab" data-tab="preorder" aria-selected="false">預約單</button>
      <button class="tab" data-tab="served" aria-selected="false">今日已出餐</button>
    </nav>

    <section class="pane" data-pane="onsite">
      <section class="stats" aria-label="現場狀態">
        <article class="stat"><span>剩餘主餐</span><strong id="remaining">0</strong></article>
        <article class="stat"><span>待出餐</span><strong id="pending">0</strong></article>
        <article class="stat"><span>預約出餐</span><strong id="preorder-count">0</strong></article>
        <article class="stat"><span>場次狀態</span><strong id="event-status">-</strong></article>
      </section>
      <div class="layout">
        <section class="panel">
          <h2>商品</h2>
          <div id="empty" class="empty">正在讀取目前場次與商品…</div>
          <div id="category-tabs" class="category-tabs" aria-label="商品分類"></div>
          <div id="products" class="products"></div>
        </section>
        <aside class="panel cart" id="cart-panel">
          <h2>正在點餐</h2>
          <p class="cart-time"><span>點餐時間</span><strong id="order-time">--:--</strong></p>
          <div class="cart-fields">
            <label class="field">客人姓名<input id="customer-name" maxlength="80" autocomplete="off" placeholder="可空白"></label>
            <label class="field">電話末三碼<input id="customer-phone-tail" maxlength="3" inputmode="numeric" autocomplete="off" placeholder="123"></label>
            <div class="field wide">付款方式<div class="payment-options" role="group" aria-label="付款方式"><button type="button" class="payment-option" data-payment-method="CASH" aria-pressed="true">現金</button><button type="button" class="payment-option" data-payment-method="LINE_PAY" aria-pressed="false">LINE Pay</button><input id="payment-method" type="hidden" value="CASH"></div></div>
            <div id="cash-calculator" class="cash-calculator wide">
              <label class="field">客人付款<input id="cash-received" type="number" min="0" step="1" inputmode="numeric" placeholder="例如 1000"></label>
              <div class="field"><span>找零</span><div class="cash-change"><span id="cash-change-label">尚未輸入</span><strong id="cash-change">NT$0</strong></div></div>
            </div>
            <label class="field wide">訂單備註<textarea id="order-notes" maxlength="500" placeholder="可空白"></textarea></label>
          </div>
          <div id="cart-items" class="cart-items"></div>
          <div class="cart-footer">
            <button class="secondary" id="clear-cart" type="button">清空</button>
            <div class="total-row"><span>總計</span><strong id="total">NT$0</strong></div>
            <button id="toggle-preorder" class="secondary reservation-toggle" type="button">改成預約單</button>
            <div id="reservation-fields" class="reservation-fields" hidden>
              <label class="field">取餐時間<select id="pickup-time"></select></label>
            </div>
            <button id="create-order" class="primary">送出到待出餐</button>
            <p id="notice" class="notice" role="status" aria-live="polite"></p>
          </div>
        </aside>
      </div>
      <button id="mobile-cart-summary" class="mobile-cart-summary" type="button" hidden>
        <span id="mobile-cart-count">購物車 0 份</span>
        <strong id="mobile-cart-total">NT$0 · 查看</strong>
      </button>
    </section>

    <section class="pane" data-pane="pending" hidden>
      <section class="panel">
        <div class="orders-toolbar">
          <div>
            <h2>待出餐</h2>
            <p class="order-meta">依建立時間排序；此處只顯示目前場次訂單。</p>
          </div>
        </div>
        <div id="orders" class="order-list"></div>
      </section>
    </section>
    <section class="pane" data-pane="preorder" hidden>
      <section class="panel">
        <div class="orders-toolbar">
          <div>
            <h2>預約單</h2>
            <p class="order-meta">依取餐時間排序；製作狀態仍由同一張中央訂單同步。</p>
          </div>
        </div>
        <div id="preorder-orders" class="order-list"></div>
      </section>
    </section>
    <section class="pane" data-pane="served" hidden>
      <section class="panel">
        <div class="orders-toolbar">
          <div>
            <h2>今日已出餐</h2>
            <p class="order-meta">客人回來查詢時使用，只查目前場次。</p>
          </div>
          <label class="field">搜尋<input id="served-search" autocomplete="off" placeholder="姓名、電話尾碼、叫號"></label>
        </div>
        <div id="served-orders" class="order-list"></div>
      </section>
    </section>
  </main>
  <script>
    const sync=window.__rosRealtime;
    const s={event:null,products:[],orders:[],cart:new Map(),busy:false,notice:null,activeTab:'onsite',activeCategory:null,expanded:new Set(),servedSearch:'',scheduled:false,collectionMethods:new Map(),collectionKeys:new Map(),collectionBusy:new Set()};
    const $=id=>document.querySelector('#'+id);
    const api=async(path,options={})=>{try{const response=await fetch(path,{headers:{'content-type':'application/json'},...options}),body=await response.json();if(!response.ok){const error=Error(body.error?.message||'請求失敗');error.code=body.error?.code;throw error}sync.recordApiSuccess();return body.data}catch(error){sync.recordApiFailure();throw error}};
    const money=value=>'NT$'+value;
    const key=()=>crypto.randomUUID();
    const escape=value=>String(value??'').replace(/[&<>'"]/g,character=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[character]));
    const setNotice=text=>{s.notice=text||null};
    const clearNotice=()=>{s.notice=null};
    const setPaymentMethod=value=>{const input=$('payment-method');input.value=value;document.querySelectorAll('[data-payment-method]').forEach(button=>button.setAttribute('aria-pressed',String(button.dataset.paymentMethod===value)));renderCashCalculator()};
    const waitMinutes=value=>Math.max(0,Math.floor((Date.now()-new Date(value).getTime())/60000));
    const clock=()=>new Date().toLocaleTimeString('zh-TW',{hour:'2-digit',minute:'2-digit'});
    const byCreated=(left,right)=>new Date(left.createdAt).getTime()-new Date(right.createdAt).getTime();
    const byServed=(left,right)=>new Date(right.servedAt||right.createdAt).getTime()-new Date(left.servedAt||left.createdAt).getTime();
    const byPickup=(left,right)=>new Date(left.scheduledPickupAt).getTime()-new Date(right.scheduledPickupAt).getTime();
    const customerAvailable=product=>Number.isSafeInteger(product.customerAvailableQuantity)?product.customerAvailableQuantity:product.remainingQuantity;
    const safetyBuffer=product=>Number.isSafeInteger(product.safetyBufferQuantity)?product.safetyBufferQuantity:0;
    function confirmSafetySale(product,nextQuantity){const available=customerAvailable(product),safe=safetyBuffer(product);if(safe<1||nextQuantity<=available)return true;return confirm('目前客人能預訂數量已歸 0，只剩安全預留量 '+safe+' 份。是否現場販售安全數量？')}
    function addToCart(product,quantity){const existing=s.cart.get(product.productId),nextQuantity=(existing?.quantity||0)+quantity;if(nextQuantity<1){s.cart.delete(product.productId);return true}if(nextQuantity>product.remainingQuantity){setNotice('商品已售完或剩餘數量不足');return false}if(!confirmSafetySale(product,nextQuantity)){setNotice('已取消販售安全預留量');return false}s.cart.set(product.productId,{...(existing||product),...product,quantity:nextQuantity,note:existing?.note||''});return true}
    function productGroups(){const groups=new Map();for(const product of s.products){const name=product.displayCategoryName||'未分類';if(!groups.has(name))groups.set(name,[]);groups.get(name).push(product)}return [...groups.entries()].sort((left,right)=>(left[1][0]?.displayCategorySortOrder??0)-(right[1][0]?.displayCategorySortOrder??0))}
    function displayStatus(order){if(order.orderStatus==='cancelled')return '已取消';if(order.orderStatus==='completed')return '已完成';if(order.productionStatus==='served')return '已出餐';if(order.productionStatus==='ready')return '可取餐';if(order.productionStatus==='preparing')return '製作中';return '待製作'}
    function paymentMethodLabel(value){if(value==='CASH')return '現金';if(value==='LINE_PAY')return 'LINE Pay';return '未填'}
    function renderNotice(){const notice=$('notice');notice.hidden=!s.notice;notice.innerHTML=s.notice?'<span>'+escape(s.notice)+'</span><button type="button" data-dismiss-notice>關閉</button>':''}
    function renderClock(){$('order-time').textContent=clock()}
    function pickupOptions(){if(!s.event)return[];const parse=value=>{const [hour,minute]=String(value).split(':').map(Number);return hour*60+minute};let start=parse(s.event.startTime),end=parse(s.event.endTime);if(end<start)end+=1440;const first=Math.ceil(start/15)*15,values=[];for(let value=first;value<=end;value+=15){const normalized=value%1440;values.push(String(Math.floor(normalized/60)).padStart(2,'0')+':'+String(normalized%60).padStart(2,'0'))}return values}
    function renderReservation(){const select=$('pickup-time'),previous=select.value,options=pickupOptions();$('reservation-fields').hidden=!s.scheduled;$('toggle-preorder').textContent=s.scheduled?'改回現場單':'改成預約單';$('create-order').textContent=s.scheduled?'建立預約':'送出到待出餐';select.innerHTML=options.map(value=>'<option value="'+value+'">'+value+'</option>').join('');if(options.includes(previous))select.value=previous}
    function cartTotal(){return [...s.cart.values()].reduce((sum,item)=>sum+item.sellingPrice*item.quantity,0)}
    function renderCashCalculator(){const panel=$('cash-calculator'),input=$('cash-received'),change=$('cash-change'),label=$('cash-change-label');if(!panel||!input)return;const isCash=$('payment-method').value==='CASH';panel.hidden=!isCash;if(!isCash)return;const received=Math.max(0,Number(input.value||0)),difference=received-cartTotal();if(!input.value){label.textContent='尚未輸入';change.textContent=money(0);return}label.textContent=difference>=0?'找零':'尚差';change.textContent=money(Math.abs(difference))}
    function renderCart(){const total=cartTotal(),itemCount=[...s.cart.values()].reduce((sum,item)=>sum+item.quantity,0);$('total').textContent=money(total);$('mobile-cart-count').textContent='購物車 '+itemCount+' 份';$('mobile-cart-total').textContent=money(total)+' · 查看';$('mobile-cart-summary').hidden=!itemCount;$('cart-items').innerHTML=[...s.cart.values()].map(item=>'<article class="cart-line"><div class="cart-main"><strong>'+escape(item.posName)+'</strong><div class="quantity"><button type="button" data-adjust="-1" data-product-id="'+item.productId+'">-</button><span>'+item.quantity+'</span><button type="button" data-adjust="1" data-product-id="'+item.productId+'" '+(item.quantity>=item.remainingQuantity?'disabled':'')+'>+</button></div><strong class="line-total">'+money(item.quantity*item.sellingPrice)+'</strong><button type="button" class="danger icon-button" data-cart="'+item.productId+'" aria-label="刪除 '+escape(item.posName)+'">刪除</button></div><label class="field">品項備註<input data-note="'+item.productId+'" maxlength="250" value="'+escape(item.note||'')+'" placeholder="可空白"></label></article>').join('')||'<p class="empty">目前沒有加入品項</p>';$('create-order').disabled=s.busy||!s.cart.size;$('clear-cart').disabled=s.busy||!s.cart.size;renderReservation();renderCashCalculator()}
    function renderProducts(){const groups=productGroups();if(s.activeCategory&&!groups.some(([name])=>name===s.activeCategory))s.activeCategory=null;$('category-tabs').innerHTML=groups.map(([name])=>'<button type="button" class="category-tab" data-category="'+escape(name)+'" aria-pressed="'+(s.activeCategory===name)+'">'+escape(name)+'</button>').join('');const visible=s.activeCategory?groups.filter(([name])=>name===s.activeCategory):groups;const card=product=>{const soldOut=product.remainingQuantity<1,available=customerAvailable(product),safe=safetyBuffer(product),cartQuantity=s.cart.get(product.productId)?.quantity||0,meta=soldOut?'售完':available<1&&safe>0?'客人可訂 0 · 安全量 '+product.remainingQuantity:'剩 '+product.remainingQuantity+' 份',cartMeta=cartQuantity?'<span class="product-cart-count"> · 購物車 '+cartQuantity+' 份</span>':'';return '<article class="product '+(soldOut?'sold-out':'')+'" data-product-id="'+product.productId+'" '+(soldOut?'':'data-card-add="'+product.productId+'"')+'><div class="product-name">'+escape(product.posName)+'</div><div class="product-price">'+money(product.sellingPrice)+'</div><div class="product-meta '+(soldOut?'soldout':available<1&&safe>0?'soldout':'')+'">'+meta+cartMeta+'</div><button type="button" data-add="'+product.productId+'" data-action="add" data-product-id="'+product.productId+'" '+(soldOut?'disabled':'')+'>'+ (soldOut?'售完':'+1')+'</button></article>'};$('products').innerHTML=visible.map(([name,items])=>'<section class="product-section"><h3>'+escape(name)+'</h3><div class="product-grid">'+items.map(card).join('')+'</div></section>').join('');const remaining=s.products.reduce((sum,product)=>sum+product.remainingQuantity,0);$('remaining').textContent=String(remaining);$('header-remaining').textContent=String(remaining);$('empty').textContent=s.products.length?'':'目前沒有可販售商品'}
    function orderItems(order){return order.items.map(item=>escape(item.posName)+' x'+item.quantity+(item.notes?'（'+escape(item.notes)+'）':'')).join('、')}
    function orderActions(order){let actions='';if(order.orderStatus==='confirmed'&&(order.productionStatus==='not_started'||order.productionStatus==='queued'))actions+='<button type="button" data-order-action="start" data-order-id="'+order.orderId+'">開始製作</button>';if(order.orderStatus==='confirmed'&&(order.productionStatus==='preparing'||order.productionStatus==='ready'))actions+='<button type="button" data-order-action="served" data-order-id="'+order.orderId+'">完成出餐</button>';actions+='<button type="button" class="ghost" data-view-order="'+order.orderId+'">'+(s.expanded.has(order.orderId)?'收合內容':'查看內容')+'</button>';actions+='<button type="button" class="ghost" disabled>編輯</button>';return actions}
    function pickupLabel(order){return order.scheduledPickupAt?new Date(order.scheduledPickupAt).toLocaleTimeString('zh-TW',{hour:'2-digit',minute:'2-digit'}):null}
    function collectionControls(order){if(order.orderStatus!=='confirmed'||order.productionStatus!=='served'||order.paymentStatus!=='unpaid')return '';const method=s.collectionMethods.get(order.orderId)||order.paymentMethod||'CASH',busy=s.collectionBusy.has(order.orderId);return '<div class="collection-panel"><strong>取餐收款 · '+money(order.grandTotal)+'</strong><div class="collection-actions" role="group" aria-label="'+escape(order.orderNumber)+' 實際付款方式"><button type="button" data-collection-method="CASH" data-order-id="'+order.orderId+'" aria-pressed="'+String(method==='CASH')+'" '+(busy?'disabled':'')+'>現金</button><button type="button" data-collection-method="LINE_PAY" data-order-id="'+order.orderId+'" aria-pressed="'+String(method==='LINE_PAY')+'" '+(busy?'disabled':'')+'>LINE Pay</button><button type="button" class="confirm-payment" data-confirm-payment="'+order.orderId+'" '+(busy?'disabled':'')+'>'+(busy?'收款確認中':'確認收款')+'</button></div></div>'}
    function orderCard(order,mode){const created=new Date(order.createdAt).toLocaleTimeString('zh-TW',{hour:'2-digit',minute:'2-digit'}),pickup=pickupLabel(order),detail=s.expanded.has(order.orderId)?'<div class="order-detail">訂單狀態：'+escape(order.orderStatus)+' · 付款狀態：'+escape(order.paymentStatus)+' · 製作狀態：'+escape(order.productionStatus)+' · 建立時間：'+escape(new Date(order.createdAt).toLocaleString())+'</div>':'',revert=mode==='served'&&order.orderStatus==='confirmed'&&order.productionStatus==='served'?'<div class="order-actions"><button type="button" data-revert-production="'+order.orderId+'">退回上一步</button></div>':'';return '<article class="order" data-order-id="'+order.orderId+'"><div class="order-top"><div><div class="order-number">'+escape(order.orderNumber)+(pickup?'<span class="pickup-badge">預約 '+escape(pickup)+'</span>':'')+'</div><div class="order-meta">點餐 '+escape(created)+(order.customerName?' · 姓名 '+escape(order.customerName):' · 姓名未填')+(order.customerPhoneTail?' · 電話尾碼 '+escape(order.customerPhoneTail):' · 電話尾碼未填')+' · '+paymentMethodLabel(order.paymentMethod)+(pickup?' · 取餐 '+escape(pickup):' · 等待 '+waitMinutes(order.createdAt)+' 分')+'</div></div><strong>'+displayStatus(order)+'</strong></div><div class="order-items">'+orderItems(order)+'</div><div class="order-meta">'+(order.notes?'訂單備註：'+escape(order.notes):'無訂單備註')+(order.servedAt?' · 出餐 '+escape(new Date(order.servedAt).toLocaleTimeString('zh-TW',{hour:'2-digit',minute:'2-digit'})):'')+'</div><div class="status-list"><span class="status">訂單：'+escape(order.orderStatus)+'</span><span class="status">付款：'+escape(order.paymentStatus)+'</span><span class="status">製作：'+escape(order.productionStatus)+'</span></div>'+detail+(mode==='active'?'<div class="order-actions">'+orderActions(order)+'</div>':revert+collectionControls(order))+'</article>'}
    function renderOrders(){const activeOrders=s.orders.filter(order=>!order.scheduledPickupAt&&order.orderStatus==='confirmed'&&order.productionStatus!=='served').sort(byCreated),preorders=s.orders.filter(order=>order.scheduledPickupAt&&order.orderStatus==='confirmed'&&order.productionStatus!=='served').sort(byPickup),servedOrders=s.orders.filter(order=>order.productionStatus==='served').sort(byServed);const search=s.servedSearch.trim().toLowerCase(),filteredServed=search?servedOrders.filter(order=>[order.orderNumber,order.customerName,order.customerPhoneTail].some(value=>String(value||'').toLowerCase().includes(search))):servedOrders;$('pending').textContent=String(activeOrders.length);$('header-pending').textContent=String(activeOrders.length);$('header-preorder').textContent=String(preorders.length);$('preorder-count').textContent=String(preorders.length);$('orders').innerHTML=activeOrders.map(order=>orderCard(order,'active')).join('')||'<p class="empty">目前沒有待出餐訂單</p>';$('preorder-orders').innerHTML=preorders.map(order=>orderCard(order,'active')).join('')||'<p class="empty">目前沒有預約單</p>';$('served-orders').innerHTML=filteredServed.map(order=>orderCard(order,'served')).join('')||'<p class="empty">今日已出餐沒有符合的訂單</p>'}
    function renderHeader(){const eventName=s.event?s.event.displayName+' · '+s.event.eventCode:'沒有 OPEN Event';const status=s.event?.status?.toUpperCase()||'CLOSED';$('event').textContent=s.event?eventName+' · '+status:'目前沒有 OPEN Event';$('header-event').textContent=eventName;$('header-status').textContent=status;$('event-status').textContent=status}
    function render(){renderHeader();renderClock();renderCart();renderProducts();renderOrders();if(s.event?.status==='paused'){document.querySelectorAll('[data-add],#create-order').forEach(button=>button.disabled=true)}renderNotice()}
    function renderTab(){document.querySelectorAll('[data-pane]').forEach(pane=>{pane.hidden=pane.dataset.pane!==s.activeTab});document.querySelectorAll('[data-tab]').forEach(tab=>tab.setAttribute('aria-selected',String(tab.dataset.tab===s.activeTab)))}
    async function load(){try{const event=await api('/api/events/current');s.event=event;sync.setEventId(event?.eventId);if(!event){s.products=[];s.orders=[];setNotice('目前沒有營業中的場次，POS 暫時無法建立訂單');render();return}const [products,orders]=await Promise.all([api('/api/events/current/products'),api('/api/events/'+event.eventId+'/orders')]);s.products=products.filter(product=>Array.isArray(product.channels)&&product.channels.includes('pos'));s.orders=orders;let cartReconciled=false;for(const [productId,item] of s.cart){const product=s.products.find(candidate=>candidate.productId===productId);if(!product||product.remainingQuantity<1){s.cart.delete(productId);cartReconciled=true;continue}const quantity=Math.min(item.quantity,product.remainingQuantity);if(quantity!==item.quantity)cartReconciled=true;s.cart.set(productId,{...item,...product,quantity})}if(cartReconciled)setNotice('商品已售完或剩餘數量不足');render()}catch(error){setNotice(navigator.onLine?'中央資料讀取失敗，系統會自動重試':'目前離線，無法連線中央系統');$('empty').textContent='暫時無法讀取商品，請等待系統重新連線';renderNotice();throw error}}
    async function updateProduction(order,action){if(s.busy)return;s.busy=true;clearNotice();render();try{if(action==='start')await api('/api/orders/'+order.orderId+'/status',{method:'PATCH',body:JSON.stringify({status:'preparing',operator:'pos'})});if(action==='served'){if(order.productionStatus==='preparing')await api('/api/orders/'+order.orderId+'/status',{method:'PATCH',body:JSON.stringify({status:'ready',operator:'pos'})});await api('/api/orders/'+order.orderId+'/status',{method:'PATCH',body:JSON.stringify({status:'served',operator:'pos'})})}setNotice(order.orderNumber+' 狀態已更新');await load()}catch(error){setNotice(error instanceof TypeError?'無法連線，請確認中央服務':error.message)}finally{s.busy=false;render()}}
    async function revertProductionCompletion(button){if(s.busy)return;if(!confirm('確定將此訂單退回前一個製作狀態？\\n此操作不會取消訂單、退款或修改庫存。'))return;s.busy=true;clearNotice();render();try{const deviceId=new URLSearchParams(location.search).get('device')||'POS';const order=await api('/api/orders/'+button.dataset.revertProduction+'/production/revert-completion',{method:'POST',body:JSON.stringify({confirmed:true,reason:'accidental_completion',operator:'pos',deviceId})});setNotice(order.orderNumber+' 已退回'+(order.productionStatus==='ready'?'待取餐':'前一個製作狀態'));await load()}catch(error){setNotice(error instanceof TypeError?'無法連線，退回操作未送達中央':'退回失敗：'+error.message)}finally{s.busy=false;render()}}
    async function confirmPayment(button){const order=s.orders.find(item=>item.orderId===button.dataset.confirmPayment);if(!order||s.collectionBusy.has(order.orderId))return;const method=s.collectionMethods.get(order.orderId)||order.paymentMethod||'CASH',idempotencyKey=s.collectionKeys.get(order.orderId)||key();s.collectionKeys.set(order.orderId,idempotencyKey);s.collectionBusy.add(order.orderId);clearNotice();renderOrders();try{const deviceId=new URLSearchParams(location.search).get('device')||'POS';const result=await api('/api/orders/'+order.orderId+'/payment/confirm',{method:'POST',body:JSON.stringify({confirmed:true,paymentMethod:method,expectedAmount:order.grandTotal,idempotencyKey,operator:'pos',deviceId})});s.collectionKeys.delete(order.orderId);s.collectionMethods.delete(order.orderId);setNotice(result.order.orderNumber+' 已確認收款 '+money(result.payment.amount));await load()}catch(error){setNotice(error instanceof TypeError?'無法連線，收款狀態尚未更新':'確認收款失敗：'+error.message)}finally{s.collectionBusy.delete(order.orderId);render()}}
    document.addEventListener('click',event=>{const target=event.target;const payment=target.closest('[data-payment-method]');if(payment){setPaymentMethod(payment.dataset.paymentMethod);return}const collectionMethod=target.closest('[data-collection-method]');if(collectionMethod){s.collectionMethods.set(collectionMethod.dataset.orderId,collectionMethod.dataset.collectionMethod);renderOrders();return}const confirmPaymentButton=target.closest('[data-confirm-payment]');if(confirmPaymentButton){void confirmPayment(confirmPaymentButton);return}const tab=target.closest('[data-tab]');if(tab){s.activeTab=tab.dataset.tab;renderTab();return}const category=target.closest('[data-category]');if(category){s.activeCategory=s.activeCategory===category.dataset.category?null:category.dataset.category;renderProducts();return}if(target.closest('[data-dismiss-notice]')){clearNotice();renderNotice();return}if(target.closest('#toggle-preorder')){s.scheduled=!s.scheduled;renderCart();return}if(target.closest('#mobile-cart-summary')){$('cart-panel').scrollIntoView({behavior:'smooth',block:'start'});return}if(target.closest('#clear-cart')){s.cart.clear();renderCart();renderProducts();return}const revert=target.closest('[data-revert-production]');if(revert){void revertProductionCompletion(revert);return}const view=target.closest('[data-view-order]');if(view){const id=view.dataset.viewOrder;if(s.expanded.has(id))s.expanded.delete(id);else s.expanded.add(id);renderOrders();return}const action=target.closest('[data-order-action]');if(action){const order=s.orders.find(item=>item.orderId===action.dataset.orderId);if(order)void updateProduction(order,action.dataset.orderAction);return}const add=target.closest('[data-add],[data-card-add]'),remove=target.closest('[data-cart]'),adjust=target.closest('[data-adjust]');if(add){const productId=add.dataset.add||add.dataset.cardAdd,product=s.products.find(item=>item.productId===productId);if(product)addToCart(product,1);renderCart();renderProducts();renderNotice()}if(remove){s.cart.delete(remove.dataset.cart);renderCart();renderProducts()}if(adjust){const product=s.products.find(candidate=>candidate.productId===adjust.dataset.productId);if(!product)return;addToCart(product,Number(adjust.dataset.adjust));renderCart();renderProducts();renderNotice()}});
    document.addEventListener('click',event=>{const link=event.target.closest('.system-links a');if(!link||!s.cart.size)return;if(!confirm('購物車尚未送出，離開會清空目前內容。確定離開嗎？'))event.preventDefault()});
    document.addEventListener('input',event=>{const input=event.target;if(input.id==='cash-received'){renderCashCalculator();return}if(input.id==='customer-phone-tail')input.value=input.value.replace(/\\D/g,'').slice(0,3);if(input.id==='served-search'){s.servedSearch=input.value;renderOrders();return}if(!input.dataset.note)return;const item=s.cart.get(input.dataset.note);if(item){item.note=input.value;s.cart.set(item.productId,item)}});
    $('create-order').onclick=async()=>{if(s.busy||!s.event||!s.cart.size)return;clearNotice();const phoneTail=$('customer-phone-tail').value.trim(),paymentMethod=$('payment-method').value,pickupTime=s.scheduled?$('pickup-time').value:null;if(phoneTail&&!/^\\d{3}$/.test(phoneTail)){setNotice('電話末三碼必須是 3 位數字');renderNotice();return}if(!paymentMethod){setNotice('請選擇付款方式');renderNotice();return}if(s.scheduled&&!pickupTime){setNotice('請選擇取餐時間');renderNotice();return}s.busy=true;renderCart();try{const deviceId=new URLSearchParams(location.search).get('device')||'POS';const order=await api('/api/orders',{method:'POST',body:JSON.stringify({source:'pos',eventId:s.event.eventId,idempotencyKey:key(),items:[...s.cart.values()].map(item=>({productId:item.productId,productVersionId:item.productVersionId,quantity:item.quantity,notes:item.note||null})),pickupTime,paymentCollected:!s.scheduled,operator:'pos',deviceId,customerName:$('customer-name').value.trim()||null,customerPhoneTail:phoneTail||null,paymentMethod,notes:$('order-notes').value.trim()||null})});s.cart.clear();s.scheduled=false;$('customer-name').value='';$('customer-phone-tail').value='';$('cash-received').value='';setPaymentMethod('CASH');$('order-notes').value='';setNotice((order.scheduledPickupAt?'預約單 ':'訂單 ')+order.orderNumber+' 建立成功');await load()}catch(error){setNotice(error instanceof TypeError?'無法連線，請確認中央服務':error.code==='INSUFFICIENT_QUANTITY'?'商品已售完或剩餘數量不足':error.code==='EVENT_NOT_OPEN'?'場次已關閉':error.message)}finally{s.busy=false;renderCart();renderNotice()}};
    if(new URLSearchParams(location.search).get('debug')!=='1')$('sync-debug-toggle').hidden=true;
    document.addEventListener('click',event=>{const target=event.target.closest('[data-add],[data-card-add],#create-order');if(s.event?.status==='paused'&&target){event.preventDefault();event.stopImmediatePropagation();setNotice('場次暫停中，POS 無法建立新訂單。');renderNotice()}},true);
    sync.registerLoad(load);renderTab();setInterval(renderClock,30000);renderClock();
  </script>
</body>
</html>`;
}
