import { renderRealtimeDebug } from "../shared/realtime-debug.js";

export function renderPos(): string {
  return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>ROS POS</title><style>body{font:16px Arial;margin:0;background:#f3f6f4;color:#173238}main{max-width:1280px;margin:auto;padding:16px}.stats,.layout{display:grid;gap:10px}.stats{grid-template-columns:repeat(3,1fr)}.layout{grid-template-columns:1fr 360px}.panel,.product{background:#fff;border:1px solid #cad6d1;padding:12px}.products{display:grid;grid-template-columns:repeat(auto-fill,minmax(150px,1fr));gap:10px}.product button,button{margin-top:6px;padding:8px;border:0;background:#165b59;color:#fff}.cart{position:sticky;top:10px}.line{display:flex;justify-content:space-between;gap:8px}.order{border-bottom:1px solid #dce5e1;padding:8px 0}input,textarea{width:100%;box-sizing:border-box;padding:7px;margin-top:4px}@media(max-width:800px){.layout,.stats{grid-template-columns:1fr}.cart{position:static}}</style></head><body><main><header><h1>POS</h1>${renderRealtimeDebug("POS")}<p id="event">Loading...</p><p><a href="/pos/statistics">今日統計／收攤核對</a></p></header><section class="stats"><div class="panel">剩餘主餐<br><strong id="remaining">0</strong></div><div class="panel">待出餐<br><strong id="pending">0</strong></div><div class="panel">預約出餐<br><strong>尚未啟用</strong></div></section><div class="layout"><section><h2>商品</h2><div id="empty"></div><div id="products"></div><h2>目前 Event 訂單</h2><div id="orders" class="panel"></div></section><aside class="panel cart"><h2>購物車</h2><div id="cart-items"></div><label>訂單備註<textarea id="order-notes"></textarea></label><strong id="total">NT$0</strong><button id="create-order">建立中央訂單</button><p id="notice"></p></aside></div></main><script>const sync=window.__rosRealtime,s={event:null,products:[],cart:new Map(),busy:false},$=id=>document.querySelector(id),api=async(p,o={})=>{try{const r=await fetch(p,{headers:{'content-type':'application/json'},...o}),b=await r.json();if(!r.ok){const e=Error(b.error?.message||'Request failed');e.code=b.error?.code;throw e}sync.recordApiSuccess();return b.data}catch(e){sync.recordApiFailure();throw e}},money=n=>'NT$'+n,key=()=>crypto.randomUUID(),escape=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));function render(){const total=[...s.cart.values()].reduce((a,x)=>a+x.sellingPrice*x.quantity,0);$('#total').textContent=money(total);$('#cart-items').innerHTML=[...s.cart.values()].map(x=>'<div class="line">'+escape(x.posName)+' <button data-adjust="-1" data-product-id="'+x.productId+'">-</button> x'+x.quantity+' <button data-adjust="1" data-product-id="'+x.productId+'" '+(x.quantity>=x.remainingQuantity?'disabled':'')+'>+</button> '+money(x.quantity*x.sellingPrice)+' <button data-cart="'+x.productId+'">刪除</button></div><input data-note="'+x.productId+'" placeholder="品項備註" value="'+escape(x.note||'')+'">').join('')||'尚未加入商品';$('#create-order').disabled=s.busy||!s.cart.size;const groups=new Map();for(const p of s.products){const name=p.displayCategoryName||'未分類';if(!groups.has(name))groups.set(name,[]);groups.get(name).push(p)}$('#products').innerHTML=[...groups.entries()].map(([name,items])=>'<section><h3>'+escape(name)+'</h3><div class="products">'+items.map(p=>'<article class="product" data-product-id="'+p.productId+'"><strong>'+escape(p.posName)+'</strong><br>'+money(p.sellingPrice)+'<br>剩餘 '+p.remainingQuantity+' 份<button data-add="'+p.productId+'" data-action="add" data-product-id="'+p.productId+'" '+(p.remainingQuantity<1?'disabled':'')+'>'+ (p.remainingQuantity<1?'售完':'加入')+'</button></article>').join('')+'</div></section>').join('');$('#remaining').textContent=s.products.reduce((a,p)=>a+p.remainingQuantity,0)}function status(o){if(o.orderStatus==='cancelled')return '已取消';if(o.orderStatus==='completed')return '已完成';if(o.productionStatus==='served')return '已出餐';if(o.productionStatus==='ready')return '可取餐';if(o.productionStatus==='preparing')return '製作中';return '待製作'}async function load(){const e=await api('/api/events/current');s.event=e;sync.setEventId(e?.eventId);if(!e){$('#event').textContent='目前沒有 OPEN Event';$('#empty').textContent='目前沒有可販售商品';$('#products').innerHTML='';$('#orders').textContent='尚無訂單';render();return}const [p,o]=await Promise.all([api('/api/events/current/products'),api('/api/events/'+e.eventId+'/orders')]);s.products=p.filter(x=>x.channels.includes('pos'));$('#event').textContent=e.displayName+' · '+e.eventCode+' · OPEN';$('#empty').textContent=s.products.length?'':'目前沒有可販售商品';$('#pending').textContent=o.filter(x=>x.orderStatus==='confirmed'&&x.productionStatus!=='served').length;$('#orders').innerHTML=o.map(x=>'<div class="order"><strong>'+escape(x.orderNumber)+'</strong> '+status(x)+'<br><small>'+x.items.map(i=>escape(i.posName)+' x'+i.quantity).join(', ')+' · '+x.orderStatus+'/'+x.paymentStatus+'/'+x.productionStatus+'</small></div>').join('')||'尚無訂單';for(const [id,x] of s.cart){const p=s.products.find(y=>y.productId===id);if(!p||p.remainingQuantity<1)s.cart.delete(id);else s.cart.set(id,{...x,...p,quantity:Math.min(x.quantity,p.remainingQuantity)})}render()}document.addEventListener('click',e=>{const a=e.target.closest('[data-add]'),c=e.target.closest('[data-cart]'),q=e.target.closest('[data-adjust]');if(a){const p=s.products.find(x=>x.productId===a.dataset.add),x=s.cart.get(p.productId);if(!x||x.quantity<p.remainingQuantity)s.cart.set(p.productId,{...p,quantity:(x?.quantity||0)+1,note:x?.note||''});render()}if(c){s.cart.delete(c.dataset.cart);render()}if(q){const x=s.cart.get(q.dataset.productId),p=s.products.find(y=>y.productId===q.dataset.productId),next=x.quantity+Number(q.dataset.adjust);if(next<1)s.cart.delete(x.productId);else if(next<=p.remainingQuantity)s.cart.set(x.productId,{...x,quantity:next});render()}});document.addEventListener('input',e=>{if(e.target.dataset.note){const x=s.cart.get(e.target.dataset.note);if(x){x.note=e.target.value;s.cart.set(x.productId,x)}}});$('#create-order').onclick=async()=>{if(s.busy||!s.event||!s.cart.size)return;s.busy=true;render();try{const d=await api('/api/orders',{method:'POST',body:JSON.stringify({source:'pos',eventId:s.event.eventId,idempotencyKey:key(),items:[...s.cart.values()].map(x=>({productId:x.productId,productVersionId:x.productVersionId,quantity:x.quantity,notes:x.note||null})),customerName:null,notes:$('#order-notes').value||null})});s.cart.clear();$('#order-notes').value='';$('#notice').textContent='訂單 '+d.orderNumber+' 建立成功';await load()}catch(e){$('#notice').textContent=e instanceof TypeError?'無法連線，訂單未送達中央':e.code==='INSUFFICIENT_QUANTITY'?'商品已售完或剩餘數量不足':e.code==='EVENT_NOT_OPEN'?'場次已關閉':e.message}finally{s.busy=false;render()}};sync.registerLoad(load)</script></body></html>`;
}
