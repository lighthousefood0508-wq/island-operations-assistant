import { renderBackOfficeNav, renderNavigationStyles, renderSystemNav } from "../shared/navigation.js";

export function renderEventsAdmin(): string {
  return `<!doctype html>
<html lang="zh-Hant">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>場次 | 荒島餐車 ROS</title>
  <style>
    ${renderNavigationStyles()}
    :root{font-family:Arial,"Noto Sans TC",sans-serif;color:#14363c;background:#f6f3ed;--green:#08796b;--deep:#294a5d;--line:#d9d0c2;--muted:#66777a;--paper:#fff;--warn:#bd4830;--soft:#edf5f2;--gold:#fff0cd}*{box-sizing:border-box}body{margin:0}main{max-width:1400px;margin:auto;padding:18px}header{border-bottom:2px solid var(--green);padding-bottom:12px}h1{margin:0;font-size:30px}h2{font-size:21px;margin:0}.subtitle,.meta{color:var(--muted);font-size:13px;margin:6px 0}.notice{min-height:24px;font-weight:800;color:#12633d}.notice.error{color:#a02e1b}.layout{display:grid;grid-template-columns:minmax(300px,.8fr) minmax(560px,1.65fr);gap:14px;margin-top:14px}.card{background:var(--paper);border:1px solid var(--line);border-radius:7px;padding:14px}.stack{display:grid;gap:10px}.form-row{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}label{display:grid;gap:5px;font-size:13px;font-weight:800;color:#36545a}input,select,button{font:inherit}input,select{width:100%;padding:9px;border:1px solid #aec0bd;border-radius:5px;background:#fff}button{padding:9px 12px;border:0;border-radius:5px;background:var(--green);color:#fff;font-weight:900;cursor:pointer}button.secondary{background:#677b80}button.warn{background:var(--warn)}button.ghost{background:var(--soft);border:1px solid #bfd2ce;color:#16484d}button:disabled,input:disabled,select:disabled{opacity:.55;cursor:not-allowed}.event-list{display:grid;gap:8px;margin-top:12px;max-height:340px;overflow:auto}.event{border:1px solid #d7e1df;border-radius:6px;padding:10px;display:flex;align-items:center;justify-content:space-between;gap:8px}.event.active{border-color:var(--green);box-shadow:0 0 0 2px rgba(8,121,107,.12)}.status{display:inline-block;padding:3px 8px;border-radius:999px;font-size:12px;font-weight:900;background:#edf0ef}.status.open{background:#d8f0df;color:#12633d}.status.paused{background:#fff0cf;color:#80530a}.status.draft{background:#e1ebff;color:#31517a}.status.closed,.status.archived{background:#f5e0da;color:#8d321e}.event-header{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.event-context{font-size:14px;font-weight:800}.actions{display:flex;gap:8px;flex-wrap:wrap;align-items:center}.helper{border-left:4px solid #d9d0c2;padding-left:10px;color:var(--muted);font-size:13px}.add-row{display:grid;grid-template-columns:minmax(220px,1fr) 110px 110px auto;gap:8px;align-items:end;margin:14px 0}.table-wrap{overflow:auto;border:1px solid var(--line);border-radius:7px;background:#fff}table{width:100%;border-collapse:collapse;min-width:1030px}th{background:var(--deep);color:#fff;text-align:left;font-size:13px;padding:10px 8px;white-space:nowrap}td{border-top:1px solid #ebe3d8;padding:9px 8px;vertical-align:middle}.item-name strong{font-size:17px}.item-name span{display:block}.number{font-weight:900}.warning{color:#9a421d}.empty{padding:16px;color:var(--muted)}.hidden{display:none!important}.save-bar{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-bottom:10px}.enabled{background:#dff1e9;color:#12633d}.disabled{background:#f3e2dc;color:#93341f}@media(max-width:900px){main{padding:12px}.layout,.form-row,.add-row{grid-template-columns:1fr}.event-header{flex-direction:column}.table-wrap{border:0}table{min-width:0}thead{display:none}tr{display:block;border:1px solid var(--line);border-radius:7px;margin:10px 0}td{display:grid;grid-template-columns:110px 1fr;gap:8px}td:before{content:attr(data-label);font-weight:900;color:#607176}}
  </style>
</head>
<body>
  ${renderSystemNav("admin")}
  <main>
    <header><h1>場次</h1><p class="subtitle">每天先建立或選擇場次，再加入本場商品、設定備貨與安全預留。場次代碼由系統依日期自動產生。</p><p id="notice" class="notice"></p></header>
    ${renderBackOfficeNav("events")}
    <div class="layout">
      <section id="event-management" class="card"><h2>建立／選擇場次</h2><form id="event-form" class="stack" style="margin-top:12px"><input id="event-id" type="hidden"><label>出車日期<input id="event-date" type="date" required></label><label>場次名稱<input id="event-name" required placeholder="例如：平鎮、龍潭、土城"></label><div class="form-row"><label>開始時間<input id="event-start" type="time" required></label><label>結束時間<input id="event-end" type="time" required></label></div><button type="submit">儲存場次</button></form><div id="events" class="event-list"></div></section>
      <section id="event-inventory" class="card"><div class="event-header"><div><h2>本場販售商品</h2><p id="event-context" class="event-context">請先選擇場次。</p></div><div id="event-actions" class="actions hidden"><button id="open-event" type="button">開始營業</button><button id="pause-event" type="button" class="ghost">暫停調整</button><button id="resume-event" type="button">恢復營業</button><button id="end-event" type="button" class="warn">結束今日販售</button><button id="archive-event" type="button" class="secondary">封存</button></div></div><p id="inventory-hint" class="helper">選擇草稿或暫停中的場次後，可加入已發布商品並設定本場備貨。</p><div id="inventory-editor" class="hidden"><form id="add-product-form" class="add-row"><label>已發布商品<select id="published-product"></select></label><label>備貨<input id="new-planned" type="number" min="0" value="0"></label><label>安全預留<input id="new-safety" type="number" min="0" value="0"></label><button type="submit">加入本場</button></form><div class="save-bar"><span class="meta">修改會先留在此畫面；按「儲存全部」後才一次寫入中央資料。</span><button id="save-all" type="button">儲存全部</button></div></div></section>
    </div>
    <section class="card" style="margin-top:14px"><div class="event-header"><div><h2>本場備貨與剩餘</h2><p class="meta">客人可訂 = 剩餘 − 安全預留；本場停用後不會出現在 POS。</p></div><button id="reload" class="ghost" type="button">重新讀取</button></div><div class="table-wrap"><table><thead><tr><th>品項</th><th>分類</th><th>售價</th><th>版本</th><th>備貨</th><th>安全預留</th><th>客人可訂</th><th>預約</th><th>現場售出</th><th>目前訂單</th><th>剩餘</th><th>狀態</th></tr></thead><tbody id="inventory"></tbody></table></div></section>
  </main>
  <script>
const state={events:[],published:[],inventory:[],orders:[],selected:null,dirty:false};
const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const api=async(path,options={})=>{const response=await fetch(path,{headers:{'content-type':'application/json'},...options});const body=await response.json();if(!response.ok)throw new Error(body.error?.message||'請求失敗');return body.data};
function message(text,error=false){const node=$('notice');node.textContent=text;node.className='notice'+(error?' error':'');setTimeout(()=>{if(node.textContent===text)node.textContent=''},4200)}
function current(){return state.events.find(event=>event.eventId===state.selected)||null}function editable(){return ['draft','paused'].includes(current()?.status)}function activeQuantity(version){return state.orders.filter(order=>!['completed','cancelled'].includes(order.orderStatus)).flatMap(order=>order.items||[]).filter(item=>item.productVersionId===version).reduce((total,item)=>total+item.quantity,0)}
function eventLine(event){return event.displayName+' · '+event.date+' '+event.startTime+'-'+event.endTime}
function renderEvents(){$('events').innerHTML=state.events.map(event=>'<div class="event '+(event.eventId===state.selected?'active':'')+'"><div><strong>'+esc(event.displayName)+'</strong> <span class="status '+event.status+'">'+esc({draft:'草稿',open:'營業中',paused:'暫停',closed:'已結束',archived:'已封存'}[event.status]||event.status)+'</span><div class="meta">'+esc(event.eventCode)+' · '+esc(event.date)+' '+esc(event.startTime)+'-'+esc(event.endTime)+'</div></div><button class="secondary" data-select="'+event.eventId+'">選取</button></div>').join('')||'<div class="empty">目前沒有場次。</div>'}
function renderContext(){const event=current(),canEdit=editable();$('event-context').textContent=event?eventLine(event)+'（'+({draft:'草稿',open:'營業中',paused:'暫停',closed:'已結束',archived:'已封存'}[event.status]||event.status)+'）':'請先選擇場次。';$('event-actions').classList.toggle('hidden',!event);$('inventory-editor').classList.toggle('hidden',!canEdit);$('inventory-hint').classList.toggle('hidden',canEdit);if(event&&!canEdit)$('inventory-hint').textContent=event.status==='open'?'營業中已鎖定備貨；請先暫停調整。':'此場次目前不可修改備貨。';for(const [id,status] of [['open-event','draft'],['pause-event','open'],['resume-event','paused'],['end-event','open'],['archive-event','draft']])$(id).classList.toggle('hidden',!event||event.status!==status);if(event&&event.status==='closed')$('archive-event').classList.remove('hidden');const used=new Set(state.inventory.map(item=>item.productVersionId));$('published-product').innerHTML='<option value="">選擇商品</option>'+state.published.filter(product=>!used.has(product.productVersionId)).map(product=>'<option value="'+product.productVersionId+'">'+esc(product.displayCategoryName)+' · '+esc(product.posName)+' · NT$'+product.sellingPrice+'</option>').join('')}
function renderInventory(){const body=$('inventory');if(!current()){body.innerHTML='<tr><td colspan="12" class="empty">請先選擇場次。</td></tr>';return}body.innerHTML=state.inventory.map((item,index)=>{const remaining=item.plannedQuantity-item.reservedQuantity-item.soldQuantity,customer=Math.max(0,remaining-item.safetyBufferQuantity),canEdit=editable();return '<tr><td data-label="品項" class="item-name"><strong>'+esc(item.posName||item.displayName)+'</strong><span class="meta">'+esc(item.displayName)+'</span></td><td data-label="分類">'+esc(item.displayCategoryName||'—')+'</td><td data-label="售價" class="number">NT$'+esc(item.sellingPrice)+'</td><td data-label="版本">v'+esc(item.contractVersion)+'</td><td data-label="備貨"><input data-index="'+index+'" data-field="planned" type="number" min="0" value="'+item.plannedQuantity+'" '+(canEdit?'':'disabled')+'></td><td data-label="安全預留"><input data-index="'+index+'" data-field="safety" type="number" min="0" value="'+item.safetyBufferQuantity+'" '+(canEdit?'':'disabled')+'></td><td data-label="客人可訂" class="number '+(customer===0?'warning':'')+'">'+customer+'</td><td data-label="預約" class="number">'+item.reservedQuantity+'</td><td data-label="現場售出" class="number">'+item.soldQuantity+'</td><td data-label="目前訂單" class="number">'+activeQuantity(item.productVersionId)+'</td><td data-label="剩餘" class="number">'+remaining+'</td><td data-label="狀態">'+(canEdit?'<button class="'+(item.isEnabled?'ghost':'warn')+'" data-toggle="'+index+'">'+(item.isEnabled?'本場停用':'恢復本場')+'</button>':'<span class="status '+(item.isEnabled?'open':'closed')+'">'+(item.isEnabled?'啟用':'本場停用')+'</span>')+'</td></tr>'}).join('')||'<tr><td colspan="12" class="empty">本場尚未加入商品。</td></tr>'}
function render(){renderEvents();renderContext();renderInventory()}
async function load(){const [events,published]=await Promise.all([api('/api/admin/events'),api('/api/catalog/products/published')]);state.events=events;state.published=published;if(state.selected&&!events.some(event=>event.eventId===state.selected))state.selected=null;render();if(state.selected)await loadEventData()}
async function loadEventData(){if(!state.selected)return;const [inventory,orders]=await Promise.all([api('/api/admin/events/'+state.selected+'/sellable-inventory'),api('/api/events/'+state.selected+'/orders')]);state.inventory=inventory;state.orders=orders;state.dirty=false;render()}
function selectEvent(event){state.selected=event.eventId;$('event-id').value=event.eventId;$('event-date').value=event.date;$('event-name').value=event.displayName;$('event-start').value=event.startTime;$('event-end').value=event.endTime;loadEventData().catch(error=>message(error.message,true))}
function batchKey(){return 'inventory-'+Date.now()+'-'+Math.random().toString(36).slice(2)}
async function saveAll(){const event=current();if(!event||!editable())throw new Error('只有草稿或暫停中的場次可調整備貨。');for(const item of state.inventory){if(item.safetyBufferQuantity>item.plannedQuantity)throw new Error('安全預留不能大於備貨。');if(item.plannedQuantity<item.reservedQuantity+item.soldQuantity)throw new Error((item.posName||item.displayName)+' 的備貨不能低於已預約與已售數量。')}const result=await api('/api/admin/events/'+event.eventId+'/sellable-inventory',{method:'PUT',body:JSON.stringify({idempotencyKey:batchKey(),operator:'Owner',items:state.inventory.map(item=>({productVersionId:item.productVersionId,plannedQuantity:item.plannedQuantity,safetyBufferQuantity:item.safetyBufferQuantity,isEnabled:item.isEnabled}))})});state.inventory=result.inventory;state.dirty=false;render();message(result.replayed?'已確認先前儲存結果。':'本場備貨已一次儲存。')}
$('event-form').addEventListener('submit',async e=>{e.preventDefault();try{const id=$('event-id').value;const body={displayName:$('event-name').value,date:$('event-date').value,startTime:$('event-start').value,endTime:$('event-end').value};const event=await api(id?'/api/admin/events/'+id:'/api/admin/events',{method:id?'PATCH':'POST',body:JSON.stringify(body)});state.selected=event.eventId;await load();selectEvent(event);message('場次已儲存：'+event.eventCode)}catch(error){message(error.message,true)}});
$('add-product-form').addEventListener('submit',e=>{e.preventDefault();try{if(!editable())throw new Error('此場次目前不可加入商品。');const product=state.published.find(item=>item.productVersionId===$('published-product').value);if(!product)throw new Error('請選擇已發布商品。');state.inventory.push({...product,plannedQuantity:Number($('new-planned').value),reservedQuantity:0,soldQuantity:0,safetyBufferQuantity:Number($('new-safety').value),remainingQuantity:0,customerAvailableQuantity:0,isEnabled:true,createdAt:'',updatedAt:''});state.dirty=true;render();message('商品已加入待儲存清單。')}catch(error){message(error.message,true)}});
$('save-all').addEventListener('click',()=>saveAll().catch(error=>message(error.message,true)));$('reload').addEventListener('click',()=>load().catch(error=>message(error.message,true)));
document.addEventListener('input',e=>{const target=e.target;if(!(target instanceof HTMLInputElement))return;const index=Number(target.dataset.index);const field=target.dataset.field;if(Number.isInteger(index)&&state.inventory[index]&&field){state.inventory[index][field==='planned'?'plannedQuantity':'safetyBufferQuantity']=Number(target.value);state.dirty=true}});
document.addEventListener('click',async e=>{const target=e.target;if(!(target instanceof HTMLElement))return;const select=target.closest('[data-select]'),toggle=target.closest('[data-toggle]');try{if(select){const event=state.events.find(item=>item.eventId===select.dataset.select);if(event)selectEvent(event);return}if(toggle){const item=state.inventory[Number(toggle.dataset.toggle)];if(item){item.isEnabled=!item.isEnabled;state.dirty=true;renderInventory()}return}}catch(error){message(error.message,true)}});
for(const [id,action] of [['open-event','open'],['pause-event','pause'],['resume-event','resume'],['archive-event','archive']])$(id).addEventListener('click',async()=>{try{const event=current();if(!event)return;if(state.dirty)throw new Error('請先儲存本場備貨調整。');const result=await api('/api/admin/events/'+event.eventId+'/'+action,{method:'POST',body:'{}'});await load();selectEvent(result);message(action==='open'?'已開始營業。':action==='pause'?'已暫停，可調整備貨。':action==='resume'?'已恢復營業。':'場次已封存。')}catch(error){message(error.message,true)}});
$('end-event').addEventListener('click',()=>{const event=current();if(!event)return;if(state.dirty){message('請先儲存本場備貨調整。',true);return}location.href='/admin/statistics?eventId='+encodeURIComponent(event.eventId)+'&closeout=1'});
load().catch(error=>message(error.message,true));
  </script>
</body>
</html>`;
}
