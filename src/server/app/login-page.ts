function safeNext(value: string | null): string { return value && value.startsWith("/") && !value.startsWith("//") ? value : "/admin"; }

/** AuthenticationRoleBoundary: minimal same-origin login UI; it owns no authorization truth. */
export function renderLoginPage(next: string | null): string {
  const target = JSON.stringify(safeNext(next));
  return `<!doctype html><html lang="zh-TW"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>ROS Login</title><style>body{font-family:system-ui;margin:3rem auto;max-width:26rem;padding:1rem;background:#f5f2ec}form{display:grid;gap:.8rem;background:#fff;padding:1.25rem;border-radius:.5rem}input,button{font:inherit;padding:.7rem}button{background:#0d796d;color:#fff;border:0;border-radius:.3rem}#notice{min-height:1.4rem;color:#9b2c2c}</style><main><h1>ROS 登入</h1><form id="login"><label>帳號<input name="login" autocomplete="username" required minlength="3"></label><label>密碼<input name="password" type="password" autocomplete="current-password" required minlength="12"></label><button>登入</button><p id="notice" role="alert"></p></form></main><script>const next=${target};document.querySelector('#login').addEventListener('submit',async event=>{event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({login:form.get('login'),password:form.get('password')})});if(response.ok){location.assign(next);return}document.querySelector('#notice').textContent='登入失敗，請確認帳號與密碼。'})</script></html>`;
}
